resources:

  genie_spaces:

    sepsis_genie:

      title: "Sepsis Genie"

      description: >
        Dummy Genie Agent used to validate deployment of the
        Sepsis Supervisor architecture.

      warehouse_id: ${var.warehouse_id}

      file_path: ${var.genie_space_file}